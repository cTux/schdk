# Встановлення залежностей

У корені репозиторію виконайте:

```powershell
pnpm install
```

Ця команда також підключає репозиторний pre-commit hook, який перевіряє
форматування та запускає лінтери перед кожним комітом.

Усі наведені нижче команди також потрібно запускати з кореня репозиторію.
