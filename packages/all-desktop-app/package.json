{
  "name": "@schdk/all-desktop-app",
  "version": "0.1.0",
  "description": "Unified desktop app for SCHDK tools.",
  "author": {
    "name": "Viacheslav Miroshko",
    "email": "ccctux@gmail.com"
  },
  "private": true,
  "type": "module",
  "main": "./dist/electron/main.js",
  "scripts": {
    "dev": "pnpm --filter @schdk/all-web-app build && pnpm build && electron .",
    "build": "tsc",
    "prepackage": "node scripts/prepare-google-oauth-client.mjs",
    "package": "electron-builder --dir",
    "package:win": "node scripts/prepare-google-oauth-client.mjs && electron-builder --win --x64",
    "package:mac": "node scripts/prepare-google-oauth-client.mjs && electron-builder --mac --x64 --arm64",
    "package:linux": "node scripts/prepare-google-oauth-client.mjs && electron-builder --linux --x64",
    "lint": "oxlint -c ../../.oxlintrc.json electron",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@schdk/ai": "workspace:*",
    "@schdk/google-drive": "workspace:*"
  },
  "devDependencies": {
    "@schdk/all-web-app": "workspace:*",
    "@types/node": "catalog:",
    "electron": "catalog:",
    "electron-builder": "catalog:",
    "oxlint": "^1.76.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "build": {
    "appId": "com.ctux.schdk.all",
    "productName": "ЩДК",
    "directories": {
      "output": "dist/release"
    },
    "electronLanguages": [
      "uk",
      "en-US"
    ],
    "files": [
      "dist/electron/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../all-web-app/dist",
        "to": "web"
      },
      {
        "from": "build/google-oauth-client.json",
        "to": "google-oauth-client.json"
      }
    ],
    "electronLanguages": [
      "uk",
      "en-US"
    ],
    "win": {
      "icon": "build/owl.png",
      "target": [
        "nsis",
        "portable"
      ]
    },
    "nsis": {
      "artifactName": "schdk-${version}-windows-${arch}-installer.${ext}"
    },
    "portable": {
      "artifactName": "schdk-${version}-windows-${arch}-portable.${ext}"
    },
    "mac": {
      "icon": "build/owl.png",
      "category": "public.app-category.games",
      "target": [
        "dir",
        "zip",
        "pkg"
      ],
      "artifactName": "schdk-${version}-macos-${arch}.${ext}"
    },
    "pkg": {
      "installLocation": "/Applications"
    },
    "linux": {
      "icon": "build/owl.png",
      "category": "Game",
      "executableName": "schdk",
      "target": [
        "dir",
        "deb"
      ],
      "artifactName": "schdk-${version}-linux-${arch}.${ext}"
    },
    "deb": {
      "packageName": "schdk",
      "synopsis": "SCHDK game package editor and host"
    }
  }
}
