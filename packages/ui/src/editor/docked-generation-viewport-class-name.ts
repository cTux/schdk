import classNames from 'classnames';

const dockedGenerationViewportClassName = classNames(
  'question-generation-viewport',
  'question-generation-viewport-docked',
);

export { dockedGenerationViewportClassName };
