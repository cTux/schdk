import type { LocalizationCopy } from '../../../localization';
import type { GameLayoutElementId } from '../../../options/types';

export interface VisualEditorPreviewProps {
  copy: LocalizationCopy;
  id: GameLayoutElementId;
}
