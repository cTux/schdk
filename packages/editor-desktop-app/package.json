{
  "name": "@schdk/editor-desktop-app",
  "version": "0.1.0",
  "description": "Desktop game-package editor.",
  "private": true,
  "type": "module",
  "main": "./dist/electron/main.js",
  "scripts": {
    "dev": "pnpm --filter @schdk/editor-web-app build && pnpm build && electron .",
    "build": "tsc && electron-builder --dir",
    "lint": "oxlint -c ../../.oxlintrc.json electron",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@schdk/editor-web-app": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "electron": "catalog:",
    "electron-builder": "catalog:",
    "oxlint": "^1.63.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "build": {
    "appId": "com.ctux.schdk.editor",
    "productName": "SCHDK Editor",
    "directories": {
      "output": "dist/release"
    },
    "files": [
      "build/**/*",
      "dist/electron/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../editor-web-app/dist",
        "to": "web"
      }
    ],
    "win": {
      "icon": "build/owl.png",
      "signExecutable": false,
      "target": "dir"
    }
  }
}
